part of '../barrel.dart';

//TODO : make stock page
class StockPage extends StatelessWidget {
  //TEMPORARY

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Icon(Icons.construction,size: 50,),
          Text('Under Construction'),
        ],
      ),
    );
  }
}
